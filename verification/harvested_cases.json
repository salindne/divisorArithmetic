{
 "cases": [],
 "note": "Frozen cases filling the branches no Magma whitebox tester reaches -- either because the family has no tester, or because the tester's own random search missed them. Regenerate with `python3 whitebox.py --harvest`. CI replays these and never samples. Expected coverage lives in coverage_baseline.json, NOT here, so a worse harvest cannot lower its own bar."
}